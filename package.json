{
  "name": "gmatch",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "yarn workspaces run test",
    "coverage": "yarn workspaces run test --coverage"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2",
    "turbo": "^2.4.4"
  },
  "dependencies": {
    "react-calendar": "^6.0.0"
  }
}
